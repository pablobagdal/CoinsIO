import 'package:flutter/material.dart';

class AccountChangeBalance extends StatelessWidget {
  const AccountChangeBalance({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
